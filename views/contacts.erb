<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Welcome to My CRM</h1>
    <nav>
      <ul>
        <li><a href="/">Homepage</a></li>
        <li><a href="/contacts">Contact List</li>
        <li><a href='/about_me'>About Me</a></li>
      </ul>
    </nav>
    <h2>My Contacts</h2>
    <ul>
      <% @contacts.each do |contact| %>
        <li>
          ID: <%= contact.id %><br>
          First name: <%= contact.first_name %> <%= contact.last_name %><br>
          <% Note.all.each do |note| %>
            <% rightnote = nil %>
            <% rightnote = "Note: " + note.body if note.contact_id == contact.id %>
            <%= rightnote %>
          <% end %>
        </li>
      <% end %>
    </ul>
  </body>
</html>
